"""OCTAVE lexer with ASCII normalization.

Stub for P1.2: lenient_lexer_with_ascii_normalization
"""


def tokenize(content: str) -> list[dict]:
    """Tokenize OCTAVE content with ASCII alias normalization.

    Args:
        content: Raw OCTAVE text

    Returns:
        List of tokens with normalization log
    """
    raise NotImplementedError("P1.2: lenient_lexer_with_ascii_normalization")
